class Animation {
    constructor() {
        this.cycles = {
            //here will be arrays of image references in order to be cycles through; 
        };
    }
    preLoad() {
        console.log('animation images preloaded')
    }
}